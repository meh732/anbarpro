[package]
name = "anbarmeh-enterprise"
version = "1.0.0"
description = "AnbarMeh Enterprise - Integrated Inventory, BOM & Production Management"
authors = ["AnbarMeh Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5", features = [] }

[dependencies]
tauri = { version = "1.5", features = [ "shell-open", "window-start-dragging", "dialog-all" ] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
